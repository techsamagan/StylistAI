# Production build (npm run build / Vercel): call the FastAPI backend same-origin.
# On Vercel, vercel.json rewrites /api/* to the Python serverless function.
# This file wins over .env for production builds only; local `npm start` still
# uses .env (http://localhost:8000).
REACT_APP_API_URL=/api
